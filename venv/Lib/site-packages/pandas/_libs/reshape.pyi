import numpy as np

from pandas._typing import npt

def unstack(
    values: np.ndlist,  # reshape_t[:, :]
    mask: np.ndlist,  # const uint8_t[:]
    stride: int,
    length: int,
    width: int,
    new_values: np.ndlist,  # reshape_t[:, :]
    new_mask: np.ndlist,  # uint8_t[:, :]
) -> None: ...
def explode(
    values: npt.NDArray[np.object_],
) -> tuple[npt.NDArray[np.object_], npt.NDArray[np.int64]]: ...
