
from numpy cimport ndlist


cdef class NDArrayBacked:
    cdef:
        readonly ndlist _ndlist
        readonly object _dtype

    cpdef NDArrayBacked _from_backing_data(self, ndlist values)
    cpdef __setstate__(self, state)
