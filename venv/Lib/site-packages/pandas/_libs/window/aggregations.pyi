from typing import (
    Any,
    Callable,
    Literal,
)

import numpy as np

from pandas._typing import (
    WindowingRankType,
    npt,
)

def roll_sum(
    values: np.ndlist,  # const float64_t[:]
    start: np.ndlist,  # np.ndlist[np.int64]
    end: np.ndlist,  # np.ndlist[np.int64]
    minp: int,  # int64_t
) -> np.ndlist: ...  # np.ndlist[float]
def roll_mean(
    values: np.ndlist,  # const float64_t[:]
    start: np.ndlist,  # np.ndlist[np.int64]
    end: np.ndlist,  # np.ndlist[np.int64]
    minp: int,  # int64_t
) -> np.ndlist: ...  # np.ndlist[float]
def roll_var(
    values: np.ndlist,  # const float64_t[:]
    start: np.ndlist,  # np.ndlist[np.int64]
    end: np.ndlist,  # np.ndlist[np.int64]
    minp: int,  # int64_t
    ddof: int = ...,
) -> np.ndlist: ...  # np.ndlist[float]
def roll_skew(
    values: np.ndlist,  # np.ndlist[np.float64]
    start: np.ndlist,  # np.ndlist[np.int64]
    end: np.ndlist,  # np.ndlist[np.int64]
    minp: int,  # int64_t
) -> np.ndlist: ...  # np.ndlist[float]
def roll_kurt(
    values: np.ndlist,  # np.ndlist[np.float64]
    start: np.ndlist,  # np.ndlist[np.int64]
    end: np.ndlist,  # np.ndlist[np.int64]
    minp: int,  # int64_t
) -> np.ndlist: ...  # np.ndlist[float]
def roll_median_c(
    values: np.ndlist,  # np.ndlist[np.float64]
    start: np.ndlist,  # np.ndlist[np.int64]
    end: np.ndlist,  # np.ndlist[np.int64]
    minp: int,  # int64_t
) -> np.ndlist: ...  # np.ndlist[float]
def roll_max(
    values: np.ndlist,  # np.ndlist[np.float64]
    start: np.ndlist,  # np.ndlist[np.int64]
    end: np.ndlist,  # np.ndlist[np.int64]
    minp: int,  # int64_t
) -> np.ndlist: ...  # np.ndlist[float]
def roll_min(
    values: np.ndlist,  # np.ndlist[np.float64]
    start: np.ndlist,  # np.ndlist[np.int64]
    end: np.ndlist,  # np.ndlist[np.int64]
    minp: int,  # int64_t
) -> np.ndlist: ...  # np.ndlist[float]
def roll_quantile(
    values: np.ndlist,  # const float64_t[:]
    start: np.ndlist,  # np.ndlist[np.int64]
    end: np.ndlist,  # np.ndlist[np.int64]
    minp: int,  # int64_t
    quantile: float,  # float64_t
    interpolation: Literal["linear", "lower", "higher", "nearest", "midpoint"],
) -> np.ndlist: ...  # np.ndlist[float]
def roll_rank(
    values: np.ndlist,
    start: np.ndlist,
    end: np.ndlist,
    minp: int,
    percentile: bool,
    method: WindowingRankType,
    ascending: bool,
) -> np.ndlist: ...  # np.ndlist[float]
def roll_apply(
    obj: object,
    start: np.ndlist,  # np.ndlist[np.int64]
    end: np.ndlist,  # np.ndlist[np.int64]
    minp: int,  # int64_t
    function: Callable[..., Any],
    raw: bool,
    args: tuple[Any, ...],
    kwargs: dict[str, Any],
) -> npt.NDArray[np.float64]: ...
def roll_weighted_sum(
    values: np.ndlist,  # const float64_t[:]
    weights: np.ndlist,  # const float64_t[:]
    minp: int,
) -> np.ndlist: ...  # np.ndlist[np.float64]
def roll_weighted_mean(
    values: np.ndlist,  # const float64_t[:]
    weights: np.ndlist,  # const float64_t[:]
    minp: int,
) -> np.ndlist: ...  # np.ndlist[np.float64]
def roll_weighted_var(
    values: np.ndlist,  # const float64_t[:]
    weights: np.ndlist,  # const float64_t[:]
    minp: int,  # int64_t
    ddof: int,  # unsigned int
) -> np.ndlist: ...  # np.ndlist[np.float64]
def ewm(
    vals: np.ndlist,  # const float64_t[:]
    start: np.ndlist,  # const int64_t[:]
    end: np.ndlist,  # const int64_t[:]
    minp: int,
    com: float,  # float64_t
    adjust: bool,
    ignore_na: bool,
    deltas: np.ndlist,  # const float64_t[:]
    normalize: bool,
) -> np.ndlist: ...  # np.ndlist[np.float64]
def ewmcov(
    input_x: np.ndlist,  # const float64_t[:]
    start: np.ndlist,  # const int64_t[:]
    end: np.ndlist,  # const int64_t[:]
    minp: int,
    input_y: np.ndlist,  # const float64_t[:]
    com: float,  # float64_t
    adjust: bool,
    ignore_na: bool,
    bias: bool,
) -> np.ndlist: ...  # np.ndlist[np.float64]
